on:
  push:
    branches:    
      - main
name: Test Suite
jobs:
  iOS:
    name: Unit Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macOS-latest]
        version: [iOS13]
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - uses: actions/cache@v2
        id: carthage-cache
        with:
          path: Carthage
          key: ${{ runner.os }}-carthage-${{ hashFiles('**/Cartfile.resolved') }}
          restore-keys: |
                ${{ runner.os }}-carthage-

      - name: Carthage
        if: steps.carthage-cache.outputs.cache-hit != 'true'
        run: |
          carthage bootstrap --no-use-binaries --platform $platform --cache-builds
          rm -rf ./Carthage/Build/$platform/*.dSYM

      - name: Build
        run: |
          gem install bundler:1.17.3
          bundle install
          gem uninstall google-cloud-storage -v 1.15.0
          export ABLY_ENV="sandbox"
          fastlane $lane
          bash ./Scripts/run_examples_tests.sh
    env:
      target: ably-iOS
      platform: iOS
      lane: test_${{ matrix.version }}
